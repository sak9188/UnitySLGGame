using UnityEngine;
using UnityEditor;

public static class SLGDefine
{
    private readonly static double CELL_SIZE = 64;
    public static float GET_CELL_SIZE()
    {
        return (float)CELL_SIZE;
    }

    // 配置存放路径
    private readonly static string CONFIG_PATH = @"/Config";





}